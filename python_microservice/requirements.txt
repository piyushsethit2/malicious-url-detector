# Core ML dependencies (Alpine-compatible versions)
torch==2.0.1
transformers==4.35.0
tokenizers==0.14.1

# Web framework
flask==2.3.3

# HTTP client (lighter alternative)
httpx==0.25.0

# Logging and utilities
python-dotenv==1.0.0

# Use standard torch instead of torch+cpu for Alpine compatibility
# Alpine will automatically use CPU-only version 